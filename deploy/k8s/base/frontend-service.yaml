apiVersion: v1
kind: Service
metadata:
  name: acme-frontend
  labels:
    app.kubernetes.io/name: acme-frontend
spec:
  selector:
    app.kubernetes.io/name: acme-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
